program Main;
class Main begin
    function Main;
        var z, y, x : Boolean;
    begin
        if z then
            if y then
                if x then
                    if z then
                        if y then
                            if x then
                                print 1;
    end
end
.

