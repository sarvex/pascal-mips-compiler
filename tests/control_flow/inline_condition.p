program Main;
class Main begin
    function Main;
    begin
        doit(true);
        doit(false);
    end
    function doit(c : Boolean);
        var a : Integer;
        var b : Integer;
        var d : Boolean;
    begin
        if c or true then begin
            print 3
        end
    end
end
.

