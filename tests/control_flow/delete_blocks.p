program Main;
class Main begin
    function Main;
        var a, b, c : Integer;
        var z, y, x : Boolean;
    begin
        if z then begin
            a := a;
            if y then
                b := b
        end else begin
            c := c;
            if y then
                b := b
        end
    end
end
.

