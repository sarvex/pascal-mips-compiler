program Main;
class Main begin
    function Main;
        var a : Integer;
        var b : Integer;
        var c : Boolean;
        var d : Boolean;
    begin
        a := (1 + 2) * 3 / (4 - 5) * 2 + 42 + 1337;
        print a;
    end
end
.

