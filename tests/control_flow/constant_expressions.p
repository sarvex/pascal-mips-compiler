program Main;
class Main begin
    function Main;
    begin
        stuff(0, 5);
        stuff(0, 10);
    end
    function stuff(a : Integer; b : Integer);
        var c : Boolean;
        var d : Boolean;
    begin
        a := (1 + 2) * 3 / (4 - 5) * -2 + 42 + -1337;
        print a;
        print 1 + b ---- 1;
    end
end
.

